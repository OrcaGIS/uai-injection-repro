name: UA-002 reachability test (vendor callable cross-org)
on: workflow_dispatch
jobs:
  call-vendor-lint:
    uses: ultimateai/workflows/.github/workflows/lint-pr.yml@main
    with:
      runner: ubuntu-latest
